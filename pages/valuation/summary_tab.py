"""Summary tab — football field, all results combined, workspace."""

import streamlit as st


def render(prepared: dict, ticker: str) -> None:
    st.subheader("Valuation Summary")
    st.info(
        "Summary tab — under construction. "
        "Football field chart combining all methods + workspace."
    )
